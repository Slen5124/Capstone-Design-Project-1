# 기여 가이드

팀 내부 협업 규칙 문서. 회의록·레퍼런스 업로드부터 코드 작업까지 동일한 규칙을 따른다.

## 브랜치 전략

> **추후 결정.** 팀 회의에서 확정한 뒤 이 절을 채운다.
>
> 그전까지는 `main`에 직접 커밋하되, 커밋 전 `git pull`로 최신 상태를 맞춘다.

## 커밋 메시지 규칙

Conventional Commits 형식을 따른다.

```
<type>: <제목>

<본문 - 선택>
```

| type | 사용처 |
| --- | --- |
| `feat` | 새 기능·분석 로직 추가 |
| `fix` | 버그 수정 |
| `docs` | 회의록, README, 레퍼런스 등 문서 |
| `data` | 데이터 스키마·전처리 파이프라인 변경 |
| `refactor` | 동작 변경 없는 구조 개선 |
| `chore` | 설정, 의존성, 빌드 등 잡무 |

예시:

```
docs: 2026-09-19 3주차 회의록 추가
feat: 존별 침투율-체류시간 2축 매트릭스 산출 함수 구현
fix: 동일 카운터 이중 집계 제거 로직 수정
```

## Pull Request

브랜치 전략이 확정되면 PR 기반으로 전환한다. PR을 올릴 때는 [PR 템플릿](.github/pull_request_template.md)의 항목을 채우고,
제목은 커밋 메시지와 동일한 형식을 쓴다.

## 회의록

- 매 회의 후 담당자가 기존 회의록의 양식에 맞춰 작성한다.
- 파일명: `docs/meetings/YYYY-MM-DD-<주제>.md` (예: `2026-09-19-weekly.md`)
- 자세한 규칙은 [docs/meetings/README.md](docs/meetings/README.md) 참고.

## 논문·레퍼런스

- 읽은 논문은 [템플릿](docs/references/TEMPLATE.md)으로 리뷰 노트를 남긴다.
- 파일명: `docs/references/<저자년도>-<짧은제목>.md` (예: `wojke2017-deepsort.md`)
- [인덱스 표](docs/references/README.md)에 한 줄 추가한다.

## 데이터 취급 원칙

1. **CCTV 영상 원본과 개인 식별 가능 데이터는 어떤 형태로든 커밋하지 않는다.**
2. 기업으로부터 받은 내부 자료(매출, 매장 도면 등)는 공개 저장소에 올리지 않는다.
3. 분석에는 집계·익명화된 데이터만 사용하고, 원본은 `data/raw/`(git 추적 제외)에 둔다.
4. 커밋 전 `git status`로 의도치 않은 데이터 파일이 포함되지 않았는지 확인한다.
